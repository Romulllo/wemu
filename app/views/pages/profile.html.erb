<div class="container container-cima justify-content-sm-center">
  <div class="row justify-content-sm-center">
    <div class="col mb-2 ml-2 p-4">
      <div class="profile-picture big-profile-picture justify-content-sm-center">
        <% avatar_url = current_user.spotify_picture_url %>
        <%= image_tag avatar_url %>
      </div>
      <div class="justify-content-sm-center">
        <h1 class="user-name text-center"><%= current_user.first_name %></h1>
        <h3 class="user-name text-center">Last song listened: <%= current_user.last_songs[0]%>, from
          <%= current_user.last_artists[0]%>
        </h3>
        <iframe src="https://open.spotify.com/embed/track/71J016NGpHvSmed7w63TzK" width="300" height="80" frameborder="0" allowtransparency="true" allow="encrypted-media"></iframe>
        <iframe src="https://open.spotify.com/embed/album/1DFixLWuPkv3KT3TnV35m3" width="300" height="80" frameborder="0" allowtransparency="true" allow="encrypted-media"></iframe>
      </div>

    </div>


<!--     <div class="col mb-2 p-2">
      <h2 class="title-profile">Last 10 Artists</h2>
        <ol class="list">
          <% current_user.last_artists.each do |artist| %>
            <li><strong><%= artist %></strong></li>
          <% end %>
        </ol>
    </div>
    <div class="col mb-2 p-2">
      <h2 class="title-profile">Last 10 Musics</h2>
        <ol class="list">
          <% current_user.last_songs.each do |song| %>
            <li><strong><%= song %></strong></li>
          <% end %>
        </ol>
    </div>
  </div> -->
  <div class="col mb-2 p-2">
      <h2 class="title-profile">Top 10 Artists</h2>
        <ol class="list">
          <% current_user.top_artists.each_with_index do |artist, index| %>
            <%= link_to current_user.link_artists[index], class: "link-tops" do %>
            <li><strong><%= artist %></strong></li>
            <% end %>
          <% end %>
        </ol>
    </div>
    <div class="col mb-2 p-2">
      <h2 class="title-profile">Top 10 Songs</h2>
        <ol class="list">
          <% current_user.top_songs.each_with_index do |song, index| %>
            <%= link_to current_user.link_songs[index], class: "link-tops" do %>
            <li><strong><%= song %></strong></li>
            <% end %>
          <% end %>
        </ol>
    </div>

</div>


<div class="container container-baixo justify-content-sm-center">
  <div class="mb-2">
    <h2 class="title-profile text-center">Last 10 Albums</h2>
    <div class="row justify-content-sm-center ">
      <% current_user.last_albums.each_with_index do |album, index| %>
        <%= link_to current_user.link_albums[index] do %>
          <div class="img-shaking col mb-2 p-2">
            <%= image_tag album %>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>
</div>


<div class="container container-cima justify-content-sm-center">
  <div class="row justify-content-sm-center">
    <div class="col mb-2 ml-2 p-4">
      <div class="profile-picture big-profile-picture justify-content-sm-center">
        <% avatar_url = current_user.spotify_picture_url %>
        <%= image_tag avatar_url %>
      </div>
      <div class="justify-content-sm-center">
        <h1 class="user-name text-center"><%= current_user.first_name %></h1>
      </div>
    </div>


    <div class="col mb-2 p-2">
      <h2 class="title-profile">Top 10 Artists</h2>
        <ol class="list">
          <% current_user.top_artists.each_with_index do |artist, index| %>
            <%= link_to current_user.link_artists[index] do %>
            <li><strong><%= artist %></strong></li>
            <% end %>
          <% end %>
        </ol>
    </div>
    <div class="col mb-2 p-2">
      <h2 class="title-profile">Top 10 Musics</h2>
        <ol class="list">
          <% current_user.top_songs.each_with_index do |song, index| %>
            <%= link_to current_user.link_songs[index] do %>
            <li><strong><%= song %></strong></li>
            <% end %>
          <% end %>
        </ol>
    </div>
  </div>
</div>
<!--
